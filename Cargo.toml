[package]
name = "rs-search"
version = "0.1.0"
edition = "2021"

[lib]
name = "rs_search"
path = "src/lib.rs"
crate-type = ["rlib"]

[profile.release]
lto = "thin"
codegen-units = 1
strip = "symbols"
